name: Build packages

# Builds every platform package and attaches them to a GitHub release when a v*
# tag is pushed. Pull requests are built as well, so changes get verified before
# they land; plain pushes to main do not trigger anything, otherwise pushing a
# commit and tagging it in one go would build the same tree twice.
#
# The packages are only considered good when the verification steps below pass:
# each platform asserts that the Qt WebEngine runtime is complete and that the
# packaged application actually starts (headless, with the offscreen platform).

on:
  push:
    tags: ['v*']
  pull_request:
  workflow_dispatch:

# Superseded pull request builds are cancelled, release builds never are.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  # Qt for the AppImage and the macOS package. These bundles carry their own Qt,
  # so nothing constrains the choice except being current: the newest stable
  # patch release also carries the newest Chromium security fixes (the AppImage
  # is the way an older distribution gets a working QML UI).
  QT_VERSION: '6.11.2'
  # Qt for the Windows installer, pinned separately on purpose: Qt 6.11
  # publishes the Windows packages in per-compiler repositories
  # (qt6_6112_msvc2022_64/ instead of qt6_6112/), which aqtinstall cannot read
  # yet - not even its newest release - so install-qt-action@v4 fails to find
  # the version. 6.10.3 is the newest release that installs; raise this to
  # 6.11.x (and drop the variable) once aqtinstall understands the new layout.
  QT_VERSION_WINDOWS: '6.10.3'
  # Qt for the .deb and the .rpm builds: the floor the QML UI needs. Qt 6.5 is
  # the minimum because the UI layer uses QQmlApplicationEngine::loadFromModule
  # (added in 6.5) and the native folder dialogs (added in 6.3). Distributions
  # that ship an older Qt (Ubuntu 22.04/24.04, Debian 12) are served by the
  # AppImage, which carries its own Qt.
  QT_VERSION_FLOOR: '6.5.3'

jobs:
  linux-appimage:
    name: Linux AppImage
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7

      - name: Determine the version
        run: |
          VERSION=$(sed -n 's/.*project(qutetavern VERSION \([0-9.]*\).*/\1/p' CMakeLists.txt)
          echo "APP_VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "version: $VERSION"

      # Qt comes from the official binaries, exactly like on Windows and macOS.
      # The distribution packages of Ubuntu 22.04 are both old (Qt 6.2) and
      # split in ways that break CMake: WebEngine needs its dictionary tool from
      # a separate package and Quick pulls in Qt6OpenGL, which is yet another
      # package. The official Qt installation is complete and has a predictable
      # layout (libexec/, resources/, translations/).
      - uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          # qtserialport is a dependency of the NMEA plugin that ships with
          # qtpositioning; without it the Qt deployment step fails
          modules: 'qtwebengine qtwebchannel qtpositioning qtserialport'
          cache: true

      - name: Install system libraries
        run: |
          sudo apt-get update
          # Headers Qt needs in order to compile, plus the runtime libraries that
          # have to end up inside the AppImage
          PACKAGES="
            build-essential cmake
            libgl1-mesa-dev libglu1-mesa-dev
            libxkbcommon-dev libxkbcommon-x11-0
            libfontconfig1-dev libfreetype6-dev
            libx11-dev libx11-xcb-dev libxcb1-dev libxcb-icccm4-dev
            libxcb-image0-dev libxcb-keysyms1-dev libxcb-randr0-dev
            libxcb-render-util0-dev libxcb-shape0-dev libxcb-sync-dev
            libxcb-xfixes0-dev libxcb-xinerama0-dev libxcb-xkb-dev libxcb-cursor0
            libnss3 libnspr4 libasound2 libdbus-1-3 libgbm1
            squashfs-tools file wget desktop-file-utils"
          # Skip names the image does not know instead of failing the whole job
          INSTALL=""
          for p in $PACKAGES; do
            if apt-cache show "$p" >/dev/null 2>&1; then
              INSTALL="$INSTALL $p"
            else
              echo "::warning::package $p is not available on this runner image"
            fi
          done
          sudo apt-get install -y --no-install-recommends $INSTALL

      - name: Configure and build
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build -j"$(nproc)"
          echo "Qt used: $QT_ROOT_DIR ($("$QT_ROOT_DIR/bin/qmake" -query QT_VERSION))"
          # A library that cannot be resolved would only surface at runtime, so
          # check it while we are still on the build machine
          if ldd build/qutetavern | grep -q "not found"; then
            ldd build/qutetavern | grep "not found"
            echo "::error::the built binary has unresolved libraries"
            exit 1
          fi

      - name: Run the unit tests
        run: ctest --test-dir build --output-on-failure

      - name: Assemble the AppDir
        run: |
          set -e
          : "${QT_ROOT_DIR:?QT_ROOT_DIR is not set by install-qt-action}"
          # The official Qt layout is fixed; if a file is missing, say so loudly
          # instead of shipping a bundle without WebEngine
          for f in libexec/QtWebEngineProcess resources/icudtl.dat \
                   translations/qtwebengine_locales; do
            if [ ! -e "$QT_ROOT_DIR/$f" ]; then
              echo "::error::$QT_ROOT_DIR/$f is missing (unexpected Qt layout)"
              ls -1 "$QT_ROOT_DIR"
              exit 1
            fi
          done

          rm -rf AppDir
          mkdir -p AppDir/usr/bin AppDir/usr/libexec AppDir/usr/translations \
                   AppDir/usr/share/applications AppDir/usr/share/icons/hicolor/256x256/apps

          install -Dm755 build/qutetavern AppDir/usr/bin/qutetavern
          cp resources/icon.png AppDir/usr/share/icons/hicolor/256x256/apps/qutetavern.png
          cp packaging/linux/qutetavern.desktop AppDir/usr/share/applications/

          # Qt WebEngine runtime: the helper process, the resources and the locale
          # packs have to sit at the paths QLibraryInfo resolves to once the Qt
          # libraries are bundled (usr/libexec, usr/resources, usr/translations)
          install -Dm755 "$QT_ROOT_DIR/libexec/QtWebEngineProcess" AppDir/usr/libexec/QtWebEngineProcess
          mkdir -p AppDir/usr/resources AppDir/usr/translations/qtwebengine_locales
          cp -a "$QT_ROOT_DIR/resources/." AppDir/usr/resources/
          cp -a "$QT_ROOT_DIR/translations/qtwebengine_locales/." AppDir/usr/translations/qtwebengine_locales/

          # license notices travel inside the bundle
          mkdir -p AppDir/usr/share/doc/qutetavern
          cp THIRD_PARTY_NOTICES.md AppDir/usr/share/doc/qutetavern/
          if [ -f LICENSE ]; then cp LICENSE AppDir/usr/share/doc/qutetavern/; fi

      - name: Bundle the shared libraries (linuxdeploy + Qt plugin)
        run: |
          set -e
          export QMAKE="$QT_ROOT_DIR/bin/qmake"
          export APPIMAGE_EXTRACT_AND_RUN=1
          # the QML sources are what tells linuxdeploy-plugin-qt which QML
          # modules to bundle (QtQuick.Controls and its styles, QtWebEngine, ...)
          export QML_SOURCES_PATHS="$PWD/qml"
          # the tools live in /tmp so that the working directory only contains
          # the artifact we build
          mkdir -p /tmp/tools
          wget -q -O /tmp/tools/linuxdeploy.AppImage \
            https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-x86_64.AppImage
          wget -q -O /tmp/tools/linuxdeploy-plugin-qt.AppImage \
            https://github.com/linuxdeploy/linuxdeploy-plugin-qt/releases/download/continuous/linuxdeploy-plugin-qt-x86_64.AppImage
          chmod +x /tmp/tools/*.AppImage
          # plugins are looked up next to the linuxdeploy binary, so keep both together
          /tmp/tools/linuxdeploy.AppImage \
            --appdir AppDir \
            --desktop-file packaging/linux/qutetavern.desktop \
            --icon-file resources/icon.png \
            --custom-apprun packaging/linux/AppRun \
            --plugin qt \
            --output appimage

      - name: Rename the artifact
        run: |
          set -e
          ARTIFACT=$(ls *.AppImage | head -1)
          mv "$ARTIFACT" "QuteTavern-${APP_VERSION}-x86_64.AppImage"
          ls -l "QuteTavern-${APP_VERSION}-x86_64.AppImage"

      - name: Verify the bundle contents
        run: |
          set -e
          test -f AppDir/usr/libexec/QtWebEngineProcess
          test -f AppDir/usr/resources/icudtl.dat
          test -d AppDir/usr/translations/qtwebengine_locales
          test -f AppDir/usr/lib/libQt6WebEngineCore.so.6
          test -x AppDir/AppRun
          echo "WebEngine helper, resources, locales and libraries are in place"
          # the QML modules the UI imports have to be inside the bundle as well
          QMLROOT=$(find AppDir -maxdepth 4 -type d -name qml | head -1)
          [ -n "$QMLROOT" ] || {
            echo "::error::no QML modules were bundled into the AppDir"
            find AppDir -maxdepth 3 -type d
            exit 1
          }
          for m in QtQuick QtQuick/Controls QtQuick/Controls/Basic QtQuick/Dialogs \
                   QtQuick/Layouts QtQuick/Templates QtQuick/Window QtQml QtWebEngine; do
            if [ ! -d "$QMLROOT/$m" ]; then
              echo "::error::the QML module $m is missing from the bundle"
              find "$QMLROOT" -maxdepth 2 -type d | head -40
              exit 1
            fi
          done
          echo "bundled QML modules: $QMLROOT"
          # nothing the bundle needs may be missing
          if LD_LIBRARY_PATH="$PWD/AppDir/usr/lib:$PWD/AppDir/usr/lib/x86_64-linux-gnu" \
               ldd AppDir/usr/libexec/QtWebEngineProcess | grep -q "not found"; then
            LD_LIBRARY_PATH="$PWD/AppDir/usr/lib:$PWD/AppDir/usr/lib/x86_64-linux-gnu" \
              ldd AppDir/usr/libexec/QtWebEngineProcess | grep "not found"
            echo "::error::the bundled QtWebEngineProcess has unresolved dependencies"
            exit 1
          fi
          echo "QtWebEngineProcess resolves every dependency inside the bundle"

      - name: Startup smoke test (headless)
        run: |
          set -e
          set +e
          QT_QPA_PLATFORM=offscreen timeout 12 \
            ./QuteTavern-${APP_VERSION}-x86_64.AppImage --appimage-extract-and-run >out.log 2>&1
          rc=$?
          set -e
          cat out.log
          if [ "$rc" != "124" ]; then
            echo "::error::the AppImage did not stay alive (exit code $rc)"
            exit 1
          fi
          echo "the AppImage started and stayed alive"

      - uses: actions/upload-artifact@v7
        # upload even when a check above failed: the artifacts are meant to be
        # tried on real machines, and a failing smoke test is no reason to hide
        # the package. The release job still requires every job to succeed.
        if: always()
        with:
          name: linux-appimage
          path: QuteTavern-*-x86_64.AppImage
          if-no-files-found: error
          # the package is compressed already, re-zipping it only costs time
          compression-level: 0

  windows-installer:
    name: Windows installer
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v7

      - name: Determine the version
        shell: bash
        run: |
          VERSION=$(sed -n 's/.*project(qutetavern VERSION \([0-9.]*\).*/\1/p' CMakeLists.txt)
          echo "APP_VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "version: $VERSION"

      - uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION_WINDOWS }}
          arch: win64_msvc2022_64
          modules: 'qtwebengine qtwebchannel qtpositioning qtserialport'
          cache: true

      - name: Configure and build
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build --config Release --parallel

      - name: Deploy the Qt runtime
        run: |
          New-Item -ItemType Directory -Force dist | Out-Null
          Copy-Item build\Release\qutetavern.exe dist\
          # windeployqt also copies QtWebEngineProcess.exe, its resources and the
          # locale packs; the compiler runtime is bundled so no VC++ redistributable
          # has to be installed by the user. --qmldir makes it scan the QML sources
          # and copy the QML modules they import (QtQuick.Controls + its styles,
          # QtWebEngine, ...): without them the app starts without a user interface.
          windeployqt --release --compiler-runtime --no-translations --qmldir qml dist\qutetavern.exe
          # license notices travel inside the installation
          Copy-Item THIRD_PARTY_NOTICES.md dist\
          if (Test-Path LICENSE) { Copy-Item LICENSE dist\ }
          Get-ChildItem dist | Select-Object -First 20 Name

      - name: Verify the deployment
        run: |
          $required = @(
            "dist\qutetavern.exe",
            "dist\Qt6Core.dll",
            "dist\Qt6WebEngineCore.dll",
            "dist\QtWebEngineProcess.exe",
            "dist\resources\icudtl.dat",
            "dist\translations\qtwebengine_locales",
            "dist\qml\QtQuick\Controls",
            "dist\qml\QtQuick\Controls\Basic",
            "dist\qml\QtQuick\Dialogs",
            "dist\qml\QtQuick\Window",
            "dist\qml\QtWebEngine"
          )
          foreach ($item in $required) {
            if (-not (Test-Path $item)) { throw "missing in the deployment: $item" }
          }
          Write-Host "the Qt WebEngine runtime and the QML modules are complete"

      - name: Startup smoke test (headless)
        run: |
          $env:QT_QPA_PLATFORM = "offscreen"
          $p = Start-Process -FilePath "dist\qutetavern.exe" -PassThru
          Start-Sleep -Seconds 10
          if ($p.HasExited) { throw "the packaged app exited immediately (exit code $($p.ExitCode))" }
          Stop-Process -Id $p.Id -Force
          Write-Host "the packaged app started and stayed alive"

      - name: Build the NSIS installer
        run: |
          choco install nsis --no-progress -y
          if (Get-Command magick -ErrorAction SilentlyContinue) {
            magick resources\icon.png -define icon:auto-resize=256,128,64,48,32,16 resources\icon.ico
          }
          # NSIS resolves relative paths against the script directory, so both
          # the source directory and the output file are passed as absolute paths
          $sourceDir = (Resolve-Path dist).Path
          $setup = Join-Path $PWD "QuteTavern-$env:APP_VERSION-Setup.exe"
          & "${env:ProgramFiles(x86)}\NSIS\makensis.exe" `
              /DVERSION=$env:APP_VERSION "/DSOURCE_DIR=$sourceDir" "/DOUTFILE=$setup" `
              packaging\windows\installer.nsi
          if (-not (Test-Path $setup)) { throw "the installer was not produced" }
          Write-Host "built $setup"

      - name: Install and uninstall test
        run: |
          $setup = "QuteTavern-$env:APP_VERSION-Setup.exe"
          Start-Process -FilePath $setup -ArgumentList "/S" -Wait
          $dir = Join-Path $env:LOCALAPPDATA "Programs\QuteTavern"
          $exe = Join-Path $dir "qutetavern.exe"
          if (-not (Test-Path $exe)) { throw "the silent installation did not install the app" }
          if (-not (Test-Path (Join-Path $dir "Uninstall.exe"))) { throw "no uninstaller was installed" }

          $env:QT_QPA_PLATFORM = "offscreen"
          $p = Start-Process -FilePath $exe -PassThru
          Start-Sleep -Seconds 10
          if ($p.HasExited) { throw "the installed app exited immediately (exit code $($p.ExitCode))" }
          Stop-Process -Id $p.Id -Force

          Start-Process -FilePath (Join-Path $dir "Uninstall.exe") -ArgumentList "/S" -Wait
          Start-Sleep -Seconds 5
          if (Test-Path $exe) { throw "the uninstaller left files behind" }
          Write-Host "install + start + uninstall round trip succeeded"

      - uses: actions/upload-artifact@v7
        # see the AppImage job: artifacts are uploaded even if a check failed
        if: always()
        with:
          name: windows-installer
          path: QuteTavern-*-Setup.exe
          if-no-files-found: error
          compression-level: 0

  macos-pkg:
    name: macOS package
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v7

      - name: Determine the version
        run: |
          VERSION=$(sed -n 's/.*project(qutetavern VERSION \([0-9.]*\).*/\1/p' CMakeLists.txt)
          echo "APP_VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "version: $VERSION"

      - uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          modules: 'qtwebengine qtwebchannel qtpositioning qtserialport'
          cache: true

      - name: Configure and build (universal binary)
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release \
                -DCMAKE_OSX_ARCHITECTURES="x86_64;arm64" \
                -DCMAKE_OSX_DEPLOYMENT_TARGET=12.0
          cmake --build build --parallel

      - name: Assemble the app bundle
        run: |
          set -e
          rm -rf dist && mkdir -p dist
          cp -R build/qutetavern.app "dist/QuteTavern.app"
          # CMake creates the bundle without a Resources directory when it holds
          # no resources yet, but the icon and the notices go in there
          mkdir -p "dist/QuteTavern.app/Contents/Resources"

          # application icon (icns generated from the shared png)
          rm -rf icon.iconset && mkdir icon.iconset
          gen() { sips -z "$1" "$1" resources/icon.png --out "icon.iconset/$2" >/dev/null; }
          gen 16 icon_16x16.png
          gen 32 icon_16x16@2x.png
          gen 32 icon_32x32.png
          gen 64 icon_32x32@2x.png
          gen 128 icon_128x128.png
          gen 256 icon_128x128@2x.png
          gen 256 icon_256x256.png
          gen 512 icon_256x256@2x.png
          gen 512 icon_512x512.png
          gen 1024 icon_512x512@2x.png
          iconutil -c icns icon.iconset -o icon.icns
          cp icon.icns "dist/QuteTavern.app/Contents/Resources/"

          PLIST="dist/QuteTavern.app/Contents/Info.plist"
          /usr/libexec/PlistBuddy -c "Set :CFBundleName 'QuteTavern'" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Set :CFBundleDisplayName 'QuteTavern'" "$PLIST" 2>/dev/null || \
            /usr/libexec/PlistBuddy -c "Add :CFBundleDisplayName string 'QuteTavern'" "$PLIST"
          /usr/libexec/PlistBuddy -c "Set :CFBundleShortVersionString '${APP_VERSION}'" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Set :CFBundleVersion '${APP_VERSION}'" "$PLIST" 2>/dev/null || true
          /usr/libexec/PlistBuddy -c "Add :CFBundleIconFile string icon.icns" "$PLIST" 2>/dev/null || \
            /usr/libexec/PlistBuddy -c "Set :CFBundleIconFile icon.icns" "$PLIST"

          # license notices travel inside the bundle (must be added before signing)
          cp THIRD_PARTY_NOTICES.md "dist/QuteTavern.app/Contents/Resources/"
          if [ -f LICENSE ]; then cp LICENSE "dist/QuteTavern.app/Contents/Resources/"; fi

      - name: Deploy the Qt frameworks
        run: |
          set -e
          # -qmldir makes macdeployqt scan the QML sources and copy the QML
          # modules they import (QtQuick.Controls + its styles, QtWebEngine, ...)
          "$QT_ROOT_DIR/bin/macdeployqt" "dist/QuteTavern.app" -qmldir=qml -verbose=1

      - name: Verify the bundle contents
        run: |
          set -e
          APP="dist/QuteTavern.app"
          test -d "$APP/Contents/Frameworks/QtWebEngineCore.framework"
          test -f "$APP/Contents/Frameworks/QtWebEngineCore.framework/Versions/A/Helpers/QtWebEngineProcess.app/Contents/MacOS/QtWebEngineProcess"
          test -f "$APP/Contents/Resources/icudtl.dat" || \
            test -f "$APP/Contents/Frameworks/QtWebEngineCore.framework/Versions/A/Resources/icudtl.dat"
          # the QML modules the UI imports must be inside the bundle as well
          QMLDIR=$(find "$APP/Contents" -maxdepth 3 -type d -name QtWebEngine -path "*qml*" | head -1)
          [ -n "$QMLDIR" ] || {
            echo "::error::the QML modules are missing from the bundle"
            find "$APP/Contents" -maxdepth 3 -type d | head -30
            exit 1
          }
          for m in QtQuick/Controls QtQuick/Controls/Basic QtQuick/Dialogs QtQuick/Window QtQml; do
            [ -d "$(dirname "$QMLDIR")/$m" ] || {
              echo "::error::the QML module $m is missing from the bundle"; exit 1; }
          done
          echo "QML modules bundled under $(dirname "$QMLDIR")"
          echo "the WebEngine framework, its helper process and the QML modules are in place"

          # The package is advertised for Intel and Apple silicon alike: the
          # build compiles both slices in one go, so the application must carry
          # both.
          ARCHS=$(lipo -archs "$APP/Contents/MacOS/qutetavern")
          echo "application architectures: $ARCHS"
          for a in arm64 x86_64; do
            case " $ARCHS " in
              *" $a "*) ;;
              *) echo "::error::the application is missing the $a slice"; exit 1 ;;
            esac
          done

      - name: Sign (ad-hoc) and verify the signature
        run: |
          set -e
          # An ad-hoc signature is mandatory on Apple silicon: unsigned binaries
          # are killed by the kernel. Replace it with a Developer ID signature
          # (and notarization) for distribution outside the project.
          codesign --force --deep --sign - "dist/QuteTavern.app"
          codesign --verify --deep --strict "dist/QuteTavern.app"
          echo "ad-hoc signature verified"

      - name: Startup smoke test (headless)
        run: |
          set -e
          APP_EXE="dist/QuteTavern.app/Contents/MacOS/qutetavern"
          QT_QPA_PLATFORM=offscreen "$APP_EXE" >smoke.log 2>&1 &
          PID=$!
          sleep 10
          if ! kill -0 "$PID" 2>/dev/null; then
            echo "::error::the packaged app exited immediately"
            cat smoke.log
            exit 1
          fi
          kill "$PID" || true
          echo "the packaged app started and stayed alive"

      - name: Build the .pkg
        run: |
          set -e
          PKG="QuteTavern-${APP_VERSION}-universal.pkg"
          # Package a destination root rather than using pkgbuild --component:
          # pkgbuild --root is the mode that accepts a component property list,
          # which is how the man page says to control "how the Installer handles
          # those bundles" in the root (man pkgbuild, COMPONENT PROPERTY LIST).
          rm -rf pkgroot
          mkdir -p pkgroot
          cp -R dist/QuteTavern.app pkgroot/QuteTavern.app
          # The installation problem this solves: with BundleIsRelocatable left
          # at its default, the package's PackageInfo carries a
          # <relocate><bundle id="..."/></relocate> element, and the Installer
          # answers it by updating a copy of the application found elsewhere on
          # the target volume instead of installing into /Applications - the
          # workspace still holds dist/QuteTavern.app and build/qutetavern.app.
          # The install reports success and /Applications stays empty.
          pkgbuild --analyze --root pkgroot component.plist
          /usr/libexec/PlistBuddy -c "Set :0:BundleIsRelocatable false" component.plist
          pkgbuild --root pkgroot \
                   --component-plist component.plist \
                   --install-location /Applications \
                   --identifier io.github.lckhot.qutetavern \
                   --version "${APP_VERSION}" \
                   "$PKG"
          ls -l "$PKG"
          echo "PKG=$PKG" >> "$GITHUB_ENV"

      - name: Inspect the package
        run: |
          set -e
          # The two decisions the packaging itself makes, checked on any machine
          # without depending on the Installer's runtime behaviour: where the
          # payload installs, and that it carries the application.
          rm -rf /tmp/qutetavern-pkg
          pkgutil --expand "$PKG" /tmp/qutetavern-pkg
          grep -q 'install-location="/Applications"' /tmp/qutetavern-pkg/PackageInfo || {
            echo "::error::the package does not install into /Applications"
            cat /tmp/qutetavern-pkg/PackageInfo
            exit 1
          }
          # A <relocate> element tells the Installer it may update a copy of the
          # application found elsewhere on the target volume instead of creating
          # the one the package declares - which is how a successful install can
          # leave /Applications empty. It comes from BundleIsRelocatable in the
          # component property list used to build the package.
          if grep -q "<relocate>" /tmp/qutetavern-pkg/PackageInfo; then
            echo "::error::the package is marked relocatable, so the installer may not use /Applications"
            grep -A3 -B1 "<relocate>" /tmp/qutetavern-pkg/PackageInfo
            exit 1
          fi
          # payload entries are listed as "./QuteTavern.app/..." (checked against
          # packages built with both pkgbuild modes), so the pattern allows the
          # optional leading "./"
          bsdtar -tf /tmp/qutetavern-pkg/Payload | grep -qE '^(\./)?QuteTavern\.app/Contents/MacOS/qutetavern$' || {
            echo "::error::the payload does not contain the application binary"
            bsdtar -tf /tmp/qutetavern-pkg/Payload | head -20
            exit 1
          }
          echo "the package installs into /Applications and carries the application"

      - name: Install and run the packaged app
        run: |
          set -e
          # The real thing a user does, so the check covers the whole path: the
          # installer accepts the package, the application lands where the
          # package declares, and it starts from there.
          sudo installer -pkg "$PKG" -target / -verbose -dumplog | tail -6
          APP="/Applications/QuteTavern.app"
          if [ ! -d "$APP" ]; then
            echo "::error::the installer did not put the application into /Applications"
            # the Installer's own record of what it did, including any decision
            # to update a copy of the application found elsewhere
            sudo pkgutil --pkg-info io.github.lckhot.qutetavern || true
            ls -la /Applications | tail -20
            sudo grep -i -e qutetavern -e relocat /var/log/install.log | tail -20 || true
            exit 1
          fi
          echo "installed at $APP"
          QT_QPA_PLATFORM=offscreen "$APP/Contents/MacOS/qutetavern" >smoke2.log 2>&1 &
          PID=$!
          sleep 10
          if ! kill -0 "$PID" 2>/dev/null; then
            echo "::error::the installed app exited immediately"
            cat smoke2.log
            exit 1
          fi
          kill "$PID" || true
          echo "the installed app started and stayed alive"

      - uses: actions/upload-artifact@v7
        # see the AppImage job: artifacts are uploaded even if a check failed
        if: always()
        with:
          name: macos-pkg
          path: QuteTavern-*-universal.pkg
          if-no-files-found: error
          compression-level: 0

  linux-packages:
    name: Linux packages (.deb + .rpm)
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v7

      - name: Determine the version
        run: |
          VERSION=$(sed -n 's/.*project(qutetavern VERSION \([0-9.]*\).*/\1/p' CMakeLists.txt)
          echo "APP_VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "version: $VERSION"

      - name: Install Qt (official binaries, ${{ env.QT_VERSION_FLOOR }})
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION_FLOOR }}
          modules: 'qtwebengine qtwebchannel qtpositioning qtserialport'
          cache: true

      - name: Install build prerequisites
        run: |
          sudo apt-get update
          # - the OpenGL/fontconfig/xkbcommon headers that Qt's CMake
          #   configuration looks for
          # - the distribution's Qt packages are deliberately NOT installed: the
          #   binary is built against the official Qt binaries, the package's
          #   dependencies are declared explicitly (make-packages.sh) and the
          #   install/start tests run in containers that provide a Qt at or above
          #   the floor version
          PACKAGES="build-essential cmake rpm
            libgl1-mesa-dev libfontconfig1-dev libfreetype6-dev
            libxkbcommon-dev libxkbcommon-x11-0 libx11-dev libx11-xcb-dev libxcb1-dev
            desktop-file-utils file wget"
          INSTALL=""
          for p in $PACKAGES; do
            if apt-cache show "$p" >/dev/null 2>&1; then
              INSTALL="$INSTALL $p"
            else
              echo "::warning::package $p is not available on this runner image"
            fi
          done
          sudo apt-get install -y --no-install-recommends $INSTALL

      - name: Configure and build
        run: |
          cmake -B build -DCMAKE_BUILD_TYPE=Release
          cmake --build build -j"$(nproc)"

      - name: Run the unit tests (on the Qt floor)
        run: ctest --test-dir build --output-on-failure

      - name: Build the .deb and the .rpm
        run: |
          bash packaging/linux/make-packages.sh build/qutetavern "${APP_VERSION}" .
          ls -l QuteTavern-${APP_VERSION}-*

      - name: Inspect the .deb
        run: |
          set -e
          DEB="QuteTavern-${APP_VERSION}-amd64.deb"
          dpkg-deb -I "$DEB"
          DEPENDS="$(dpkg-deb -f "$DEB" Depends)"
          echo "Depends: $DEPENDS"
          # the QML UI links Qt Quick / WebEngine Quick, and the QML modules are
          # loaded at runtime, so they cannot be inferred from the shared libraries
          for p in libqt6quick6 libqt6qml6 libqt6quickcontrols2-6 libqt6webenginequick6; do
            echo "$DEPENDS" | grep -q "$p" \
              || { echo "::error::$p is not declared in Depends"; exit 1; }
          done
          # Every Qt module the UI imports needs its corresponding qml6-module-*
          # package, and nothing else can be relied on to pull it in (Debian's
          # qml6-module-qtquick-controls, for instance, does not depend on
          # qtquick-dialogs). The list is therefore derived from the import
          # statements themselves, so adding an import without declaring its
          # package fails here.
          IMPORTS=$(grep -h '^import ' qml/*.qml | awk '{print $2}' | grep -vx QuteTavern | sort -u)
          echo "QML imports: $(echo $IMPORTS)"
          echo "$IMPORTS" > qml-imports.txt
          for m in $IMPORTS; do
            p="qml6-module-$(echo "$m" | tr 'A-Z.' 'a-z-')"
            echo "$DEPENDS" | grep -q "$p" \
              || { echo "::error::$m is imported by the UI but $p is not in Depends"; exit 1; }
          done
          if echo "$DEPENDS" | grep -q "libqt6widgets6"; then
            echo "::error::the package still depends on Qt Widgets"
            exit 1
          fi
          echo "Depends declares the Qt libraries and the QML modules the UI imports"

      - name: Install and start the .deb in a clean Debian container
        run: |
          set -e
          # what a Debian user does: apt installs the package and pulls Qt and the
          # QML modules from the distribution (trixie ships Qt 6.8, above the floor)
          docker run --rm -v "$PWD:/src" -w /src debian:trixie-slim bash -euc '
            apt-get update
            apt-get -y install --no-install-recommends ./QuteTavern-*.deb
            test -x /usr/bin/qutetavern
            # the WebEngine runtime and the QML modules must come in through the
            # dependencies - the layout differs per distribution, so search
            QTPROC=$(find /usr/lib -name QtWebEngineProcess -type f 2>/dev/null | head -1)
            [ -n "$QTPROC" ] || { echo "the Qt WebEngine runtime is missing"; exit 1; }
            echo "WebEngine helper: $QTPROC"
            QMLROOT=$(find /usr/lib -maxdepth 4 -type d -name QtWebEngine -path "*qml*" | head -1)
            [ -n "$QMLROOT" ] || { echo "the QtWebEngine QML module is missing"; exit 1; }
            QMLBASE=$(dirname "$QMLROOT")
            # every module the UI imports, derived from the import statements in
            # the previous step: apt has to have brought all of them in
            while read -r m; do
              d=$(echo "$m" | tr '.' '/')
              test -f "$QMLBASE/$d/qmldir" || {
                echo "the QML module $m is missing ($QMLBASE/$d/qmldir)"; exit 1; }
            done < /src/qml-imports.txt
            echo "QML modules: $QMLBASE"
            set +e
            QT_QPA_PLATFORM=offscreen timeout 12 qutetavern >run.log 2>&1
            rc=$?
            set -e
            cat run.log
            [ "$rc" = 124 ] || {
              echo "the installed application did not stay alive (exit code $rc)"; exit 1; }
            apt-get -y remove qutetavern
            echo "the .deb installs, starts and uninstalls cleanly"
          '

      - name: Inspect the .rpm
        run: |
          set -e
          RPM="QuteTavern-${APP_VERSION}-x86_64.rpm"
          rpm -qpi "$RPM"
          echo "--- requirements declared by the package ---"
          rpm -qpR "$RPM" | sort -u
          rpm -qpR "$RPM" | grep -q "libQt6Qml.so.6" \
            || { echo "::error::the Qt QML dependency is not declared"; exit 1; }
          rpm -qpR "$RPM" | grep -q "libQt6WebEngineQuick.so.6" \
            || { echo "::error::the WebEngine Quick dependency is not declared"; exit 1; }
          # libQt6Quick.so.6 is deliberately not asserted: the QML engine loads
          # Qt Quick at runtime, nothing references its symbols, so the linker
          # drops the dependency. libQt6Qml.so.6 is what carries it in: on
          # Fedora that SONAME comes from qt6-qtdeclarative, the package that
          # also ships the QtQuick QML modules.
          if rpm -qpR "$RPM" | grep -q "libQt6Widgets.so.6"; then
            echo "::error::the package still depends on Qt Widgets"
            exit 1
          fi

      - name: Install and start the .rpm in a clean Fedora container
        run: |
          set -e
          # what a user on an RPM distribution does: dnf installs the package
          # and pulls its Qt dependencies from the distribution
          docker run --rm -v "$PWD:/src" -w /src fedora:latest bash -euc '
            dnf -y install ./QuteTavern-*.rpm
            test -x /usr/bin/qutetavern
            # the WebEngine runtime must be pulled in through the dependencies
            test -e /usr/lib64/qt6/libexec/QtWebEngineProcess || {
              echo "the Qt WebEngine runtime is missing"; exit 1; }
            # ... and so must the QML modules, which the QML engine loads at
            # runtime: they are invisible to the shared library dependencies. The
            # list comes from the import statements, derived in the .deb step.
            while read -r m; do
              d=$(echo "$m" | tr '.' '/')
              test -f "/usr/lib64/qt6/qml/$d/qmldir" || {
                echo "the QML module $m is missing"; exit 1; }
            done < /src/qml-imports.txt
            echo "the QML modules are installed"
            set +e
            QT_QPA_PLATFORM=offscreen timeout 12 qutetavern >run.log 2>&1
            rc=$?
            set -e
            cat run.log
            [ "$rc" = 124 ] || {
              echo "the installed application did not stay alive (exit code $rc)"; exit 1; }
            dnf -y remove qutetavern
            echo "the .rpm installs, starts and uninstalls cleanly"
          '

      - uses: actions/upload-artifact@v7
        # see the AppImage job: artifacts are uploaded even if a check failed
        if: always()
        with:
          name: linux-packages
          path: |
            QuteTavern-*-amd64.deb
            QuteTavern-*-x86_64.rpm
          if-no-files-found: error
          compression-level: 0

  release:
    name: Publish release
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [linux-appimage, linux-packages, windows-installer, macos-pkg]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v8
        with:
          path: artifacts

      - name: Attach the packages to the release
        env:
          GH_TOKEN: ${{ github.token }}
          # without a checkout gh cannot infer the repository from a remote
          GH_REPO: ${{ github.repository }}
        run: |
          set -e
          TAG="${GITHUB_REF_NAME}"
          echo "publishing $TAG in $GH_REPO"
          find artifacts -type f
          if ! gh release view "$TAG" >/dev/null 2>&1; then
            gh release create "$TAG" --title "QuteTavern ${TAG}" --generate-notes
          fi
          find artifacts -type f -print0 | xargs -0 gh release upload "$TAG" --clobber
          gh release view "$TAG" --json assets --jq '.assets[].name'